apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ecom-pvc
  namespace: ecommerce-prod 
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard

