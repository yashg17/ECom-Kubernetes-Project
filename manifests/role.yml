apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: ecom-role
  namespace: ecommerce-prod
rules:
- apiGroups:
  - ""
  resources:
  - pods
  - configmaps
  verbs:
  - get
  - list
  - watch
